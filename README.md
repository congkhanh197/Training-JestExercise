# Training-JestExercise

The first, install dependence by this command:
>yarn install

To run test, you can use this command:
>yarn test

To see test coverage, you can use this command:
>yarn test:coverage